# GitHub import

Create an empty GitHub repository named `snapworth`, then upload/push the contents of this folder.

Recommended visibility while testing: **Private**.

After GitHub contains the project, Render can deploy directly from the repository using the included `render.yaml` and `Dockerfile`.

Never add `.env` or API keys to GitHub. Configure secrets in the hosting provider instead.
